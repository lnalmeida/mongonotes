@using MongoNotes.ModelViews
@using MongoDB.Driver.Linq
@model IEnumerable<NoteViewModel>
@{
    ViewData["Title"] = "Notes";
}

<div class="text-center">
    <h1 class="display-4">MongoNotes</h1>
    <br/>
    <br/>
    <br/>
    
    <table class="table table-striped">
      <thead>
        <tr>
          <th scope="col">Data</th>
          <th scope="col">Título</th>
          <th scope="col">Ações</th>
        </tr>
      </thead>
      <tbody>
      @foreach (var note in Model)
      {
          <tr>
              <th scope="row">@note.Date.ToString("dd/MM/yyyy")</th>
              <td>@note.Title</td>
              <td>
                  <a class="btn btn-sm fw-bold" style="color: #000; background: #ffc720;" asp-action="" asp-route-id="">
                      <i class="fa-solid fa-pencil"></i>
                      Edit
                  </a>
                  <a class="btn btn-sm fw-bold" style="background: #ce0a31; color: white;" data-bs-toggle="modal" data-bs-target="#confirmDeleteModal">
                      <i class="fa-solid fa-trash-can"></i>
                      Delete
                  </a>
              </td>
          </tr>
      }
      </tbody>
    </table>
</div>